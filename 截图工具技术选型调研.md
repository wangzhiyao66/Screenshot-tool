# 轻量级跨平台截图工具（macOS + Windows）技术选型调研

> 调研时间：2026-08。数值来自公开实测/厂商文档，不同来源差异较大，已标注来源与可信度；**未查证**项已明确标出，未编造 API 与版本号。

## 一、桌面框架对比（截图工具场景）

| 方案 | 安装包典型值 | 冷启动内存 | 跨平台一致性 | 调用原生截图 API | 生态/库 | 已知案例 |
|---|---|---|---|---|---|---|
| **Tauri 2.0**（Rust + 系统 WebView） | 3–10 MB（实测 exe 4.8MB / dmg 5.2MB） | 30–50 MB（实测 36MB） | 中（WebKit vs WebView2 有渲染差异） | 好：Rust crate `xcap`（macOS=ScreenCaptureKit / Windows=WGC+GDI）、`scap` 直调原生 | `tauri-plugin-screenshots`（底层 xcap，v2.2.0）、`global-shortcut`、`updater` | Coco AI（用该插件）；未查证到头部截图工具用 Tauri |
| **Electron** | 80–150 MB（实测 142/156MB） | 118–300 MB | 最好（自带 Chromium） | 弱：`desktopCapturer`+`MediaRecorder`，底层不可控；常外挂 Swift/C++ helper | 最成熟 | eSearch；giffrey（录 GIF，内嵌 Swift + ScreenCaptureKit helper） |
| **Flutter Desktop** | 30–60 MB | 默认 180–250 MB，优化后 ~95 MB | 高（Skia 自绘） | 差：需 Platform Channel / FFI 自己写两端原生代码 | 桌面插件稀缺，托盘/菜单/拖拽需自写 | Canonical Ubuntu 安装器；未查证到截图工具案例 |
| **Qt（C++/PySide6）** | Snipaste macOS 包 137 MB（含 Qt 全量） | 低（横评称 Snipaste ~15MB，**未查证**） | 高 | **最好**：C++ 直接调用两端 SDK，无桥接损耗 | 成熟；Qt 授权 LGPLv3/GPL 或商业 | **Snipaste（C++/Qt）、Flameshot（C++/Qt）、xland/ScreenCapture（Qt，含长截图+贴图）** |
| **.NET WPF/WinUI + Mac 原生** | Windows 端数 MB–数十 MB（self-contained 更大） | 低 | 差（两套代码两套 UI） | 好（P/Invoke + WinRT） | 极好（Windows 侧） | ShareX（C#/.NET，Windows 单端） |
| **原生双端（Swift + C#）** | 最小（Swift 10–30MB） | 最低 | 差（双倍人力） | **最好**，系统新 API 首日可用 | 系统原生 | CleanShot X、Shottr（Swift）；ShareX（C#） |
| **Wails（Go）** | 15–20 MB（社区数据） | 50–100 MB（中文博客，**未查证**） | 中 | 一般：需 cgo 或 Go 绑定，截图类绑定少 | 最小 | 未查证到截图工具类案例 |

**结论**：截图工具=「薄 UI + 重系统调用」，UI 层用 Web 技术收益最大、风险最低。**首选 Tauri 2.0**（体积/内存达标，xcap 已封装两端原生截图）；若团队已有 C++/Qt 积累，直接走 Snipaste 路线更可控。Electron 与「轻量」诉求直接冲突，不推荐。

## 二、macOS 截屏

| API | 最低系统 | 捕获单窗口 | 权限 | 性能/特点 |
|---|---|---|---|---|
| **ScreenCaptureKit**（`SCShareableContent`/`SCContentFilter`/`SCStream`/`SCStreamConfiguration`） | macOS 12.3+ | ✅ 窗口/应用/显示器，可排除指定窗口 | 需 Screen Recording + `NSScreenCaptureUsageDescription` | WWDC22 数据：同场景 CGWindowList 7fps → SCK 60fps，内存 -15%、CPU -50%；帧缓存在 GPU，支持 HDR/光标开关/帧率/像素格式 |
| **`SCScreenshotManager`**（`captureImage`/`captureSampleBuffer`） | macOS **14.0+** | ✅（配合 `SCContentFilter`） | 同上 | 静态截图首选，替代 `CGWindowListCreateImage`；注意社区反馈 `captureResolution = .best` 仍可能返回逻辑分辨率 1920×1080，需自行乘 `backingScaleFactor` 设 `width/height`（**未查证**官方结论） |
| `CGWindowListCreateImage` | 10.5+，头文件已标 `SCREEN_CAPTURE_OBSOLETE(10.5,14.0,15.0)` | ✅（按 `CGWindowID`，`kCGWindowImageBestResolution`） | 需（macOS 15 起可能反复弹隐私窗） | CPU 高、阻塞主线程；最小化/隐藏窗口抓不到；不含光标 |
| `CGDisplayCreateImage` | — | ❌ 仅整屏 | 需 | 自动忽略 `NSWindowSharingNone`；强制含光标；开销大 |
| `AVCaptureScreenInput` | macOS 10.7+ | ❌ 仅整屏 | 需 | 可配 `scaleFactor`、光标开关，可达 60fps |

- **TCC 权限**：`CGPreflightScreenCaptureAccess()` 检测、`CGRequestScreenCaptureAccess()` 申请。**授权后必须完全退出（Cmd+Q）重启才生效**，仅关窗口无效（多来源一致）。排障：`tccutil reset ScreenCapture [bundle-id]`；跳转：`x-apple.systempreferences:com.apple.preference.security?Privacy_ScreenCapture`。
- **免权限路径**：`SCContentSharingPicker`（系统级选择器）理论上由用户交互授权即可捕获、无需 Screen Recording 权限（WWDC23 介绍，开发者实测 beta 有 bug，**建议自测**）。
- **App Store 沙盒下的限制：未查证**；已知相关 entitlement `com.apple.developer.persistent-content-capture`（VNC 类持久捕获）。
- **悬停识别**：窗口级用 `CGWindowListCopyWindowInfo(.optionOnScreenOnly, kCGNullWindowID)` 取 `kCGWindowBounds`/`kCGWindowOwnerPID`/`kCGWindowNumber`（无需权限）；控件级需 Accessibility 权限 + `AXUIElementCopyElementAtPosition`（`AXIsProcessTrusted()` 检查）。
- **全局快捷键**：Carbon `RegisterEventHotKey`（**无需权限**）> `NSEvent.addGlobalMonitorForEvents`（需辅助功能，只能观察不能拦截）> `CGEventTap`（需辅助功能/输入监控，可拦截）。
- **常见坑**：① 三套坐标系——`NSScreen.frame`（左下原点，points）、`kCGWindowBounds`（左上原点，points）、SCDisplay/截图（物理像素，`CGDisplayPixelsWide`）；② 副屏在主屏左侧时坐标为负；③ Retina 用 `backingScaleFactor` 换算，推荐"整屏原生分辨率抓取 + 内存裁剪"而非让 SCK 直接抓选区；④ 刘海屏用 `NSScreen.safeAreaInsets`；⑤ Spaces/全屏窗口需用 `optionOnScreenOnly` 过滤并监听显示器变化通知。

## 三、Windows 截屏

| 方案 | 最低系统 | 单窗口 | 管理员 | DX/游戏 | 特点 |
|---|---|---|---|---|---|
| **Windows.Graphics.Capture**（`GraphicsCaptureSession`、`Direct3D11CaptureFramePool`、`GraphicsCapturePicker`） | Win10 引入（**社区对 1803 / 1903 说法不一，以 `GraphicsCaptureSession.IsSupported()` 运行时判断为准**） | ✅ 窗口/显示器 | ❌ 不需要 | ✅ | 官方推荐；可录最小化/被遮挡窗口；系统画黄框；HDR 屏建议全流程 `DXGI_FORMAT_R16G16B16A16_FLOAT` 避免过曝（MS 文档明确）；WinUI3/Win32 需 `InitializeWithWindow.Initialize(picker, hwnd)` |
| **DXGI Desktop Duplication**（`IDXGIOutputDuplication`） | Win8+ | ❌ 只能整屏后裁剪 | ❌ | ✅ 最佳（OBS 采用） | 1080p 约 2–8ms，GPU 直取；`GetFrameDirtyRects` 做脏矩形 |
| **GDI `BitBlt`** | 全版本 | ✅ | ❌ | ❌ 常黑屏/撕裂 | 20–50ms@1080p，CPU 高；高 DPI 未声明感知会模糊/错位 |
| **`PrintWindow`**(+`PW_RENDERFULLCONTENT`) | — | ✅ 后台窗口 | ❌ | ❌ 常黑 | 兜底方案 |
| Magnification API | — | ✅ | ❌ | ✅ | 可抓被遮挡窗口，但开销高、依赖 DWM |

- **高 DPI**：进程启动时 `SetProcessDpiAwarenessContext(DPI_AWARENESS_CONTEXT_PER_MONITOR_AWARE_V2)`（Win10 1703+），运行时用 `GetDpiForWindow`（Win10 1607+）；坐标换算 `PhysicalToLogicalPoint`/`LogicalToPhysicalPoint`。未声明感知时 `GetSystemMetrics(SM_CXVIRTUALSCREEN)` 与 BitBlt 结果都会被虚拟化缩放 → 截图错位。**工程实践：全屏按物理像素截图 + 内存裁剪**，绕开 `CopyFromScreen` 被二次缩放的问题。
- **智能识别**：`WindowFromPoint`=最深子窗口 HWND，`RealChildWindowFromPoint`=最浅（Raymond Chen 官方对比表）；UI Automation 用 `IUIAutomation::ElementFromPoint` / `ElementFromHandle` / `TreeWalker` / `FindFirst`，可拿 Name、ControlType、AutomationId、BoundingRectangle，覆盖 Win32/WinForms/WPF/Qt/Chrome/Electron（Chrome 需 `--force-renderer-accessibility`）；DirectUI 自绘程序可能完全不支持。**推荐组合：EnumWindows 枚举候选 → WindowFromPoint 命中 HWND → UIA 细化控件级。**
- **RegisterHotKey 限制**：仅支持「修饰符 + 单 VK」；已被系统/其他程序占用则注册失败；注册后**独占该组合**（其他程序收不到）；不支持多级序列。替代：`WH_KEYBOARD_LL`（`SetWindowsHookEx`）或 Raw Input；注意前台是提权窗口时，非管理员/非 `uiAccess` 进程无法拦截其输入。
- **常见坑**：① 多显示器负坐标（`SM_XVIRTUALSCREEN`）；② 缩放非 100% 时错位（见上）；③ HDR/10bit 需 R16G16B16A16_FLOAT 管线；④ 捕获 UAC 提权窗口自身需管理员；⑤ **以管理员运行时拖拽（OLE drag&drop）失效**——UIPI 限制，常规做法是主进程不提权、按需拉起提权子进程，或签名 + `uiAccess` + `ChangeWindowMessageFilter`（细节**未查证**）。

## 四、附加能力

**长截图（两端同思路，无成熟跨平台库）**：选区 → 模拟滚轮 → 逐帧抓 → 重叠检测 → 拼接。
```
loop { frame = capture(region); dy = findOverlap(prev, frame)  // SSD / Sobel+归一化互相关
       if dy == 0: break;  scroll(wheel, dy);  sleep(settle≈100ms);  stitch(prev, frame, dy) }
```
- Windows：`SendInput` 滚轮 + WGC/BitBlt。参考开源：xland/ScreenCapture（Qt，SSD + QtConcurrent 并行匹配，98ms settle）、scrollshot（Rust，Sobel 梯度 + 模板匹配 + 正文列检测）。
- macOS：`CGEventCreateScrollWheelEvent` 模拟滚轮 + SCK 抓帧；或读 `AXUIElement` 滚动值。**成熟的 macOS 长截图开源库：未查证到**。

**录屏 / GIF**：
- macOS：SCK → `AVAssetWriter`。三个已在生产踩过的坑：设 `movieFragmentInterval` 防止崩溃丢整文件；SCK 节能机制导致静态画面时长算成 1 秒 → 停止时补写最后一帧；**SCK 窗口捕获模式在副屏会丢失右键菜单等子窗口**（fatbobman 博客，建议改用显示器/区域模式）。
- Windows：Desktop Duplication 或 WGC → Media Foundation / ffmpeg。
- 省事方案：`ffmpeg`（静态库体积大，注意 GPL/LGPL 合规）、`gifski`（Rust，质量最优）、Rust `gif` + `imagequant`。具体体积数据**未查证**。

**OCR 选型**：

| 方案 | 体积增量 | 中文准确率 | 备注 |
|---|---|---|---|
| Apple Vision（`VNRecognizeTextRequest`） | **0** | 好 | macOS 10.15+，支持中英日 |
| Windows OCR（`Windows.Media.Ocr`） | **0** | 中-好 | Win10+，需系统已装语言包 |
| PaddleOCR（PP-OCRv4/mobile） | ~8–17 MB | 最高（92–97%） | 中文场景首选，可按需下载 |
| Tesseract 5 | 语言包数十 MB | 85–90%，较慢 | 复杂背景下降明显 |

建议：默认走系统原生 OCR（零体积），「高精度中文」作为可选模型下载。

**贴图/置顶**：macOS 用 `NSWindow`（`styleMask: .borderless`、`level: .floating/.screenSaver`、`collectionBehavior: [.canJoinAllSpaces, .fullScreenAuxiliary]`、`ignoresMouseEvents` 做穿透）；Windows 用 `WS_EX_TOOLWINDOW | WS_EX_LAYERED` + `SetWindowPos(HWND_TOPMOST)`。选区交互统一做法：**每个显示器一个全屏透明无边框覆盖层**。

**打包分发**：
- macOS：`codesign --options runtime --timestamp` → `xcrun notarytool submit --wait`（通常 1–15 分钟）→ `xcrun stapler staple`。更新用 **Sparkle 2**（支持 delta 更新）或 Tauri updater。
- Windows：MS 官方口径——**EV 证书自 2024 起不再即时绕过 SmartScreen**；推荐 Azure Artifact Signing（约 $9.99/月，限美/加/欧盟/英国组织；个人限美加），否则 OV 证书 $150–300/年（组织验证 1–3 天）。`signtool` 务必加 RFC3161 时间戳。更新：**Velopack**（Squirrel 的活跃继任）、Squirrel.Windows、WinSparkle。

## 主要来源
- Apple: [ScreenCaptureKit](https://developer.apple.com/documentation/screencapturekit) · [SCScreenshotManager(macOS 14+)](https://developer.apple.com/documentation/screencapturekit/scscreenshotmanager) · [NSScreen](https://developer.apple.com/documentation/appkit/nsscreen)
- MS Learn: [Screen capture (Windows.Graphics.Capture)](https://learn.microsoft.com/en-us/windows/uwp/audio-video-camera/screen-capture) · [Code signing options](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/code-signing-options) · [Obtaining UI Automation Elements](https://learn.microsoft.com/en-us/windows/win32/winauto/uiauto-obtainingelements)
- OBS PR #5875（SCK vs CGWindowList 实测）· [nonstrict: SCK on Sonoma](https://nonstrict.eu/blog/2023/a-look-at-screencapturekit-on-macos-sonoma) · [fatbobman: SCK 录制踩坑](https://fatbobman.com/en/posts/screensage-from-pixel-to-meta) · [Raymond Chen: WindowFromPoint 家族对比](https://devblogs.microsoft.com/oldnewthing?p=11873) · [xcap](https://github.com/nashaofu/xcap) · [scap](https://github.com/CapSoftware/scap) · [tauri-plugin-screenshots](https://github.com/ayangweb/tauri-plugin-screenshots) · [scrollshot](https://github.com/xutianyi1999/scrollshot) · [xland/ScreenCapture 长截图](https://deepwiki.com/xland/ScreenCapture/3.3-long-screenshots-(winlong))
