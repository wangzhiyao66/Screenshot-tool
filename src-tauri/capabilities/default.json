{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Shotly 默认权限",
  "windows": ["main", "editor", "overlay-*", "pin-*"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default",
    "dialog:default",
    "global-shortcut:default"
  ]
}
